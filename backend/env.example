# Database Configuration
# For SQLite (development):
SQLALCHEMY_DATABASE_URL=sqlite:///./sql_app.db

# For PostgreSQL (production):
# SQLALCHEMY_DATABASE_URL=postgresql://user:password@localhost:5432/gitto_db

# For Railway/Render/Other cloud providers:
# SQLALCHEMY_DATABASE_URL will be provided automatically as an environment variable

# CORS Configuration
# Comma-separated list of allowed origins
# Production example: CORS_ORIGINS=https://app.gitto.ai,https://www.gitto.ai
# Development default: http://localhost:3000,http://localhost:3001
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Debug Logging
# Optional: Custom path for debug logs (relative or absolute)
# Default: .cursor/debug.log in current working directory
DEBUG_LOG_PATH=.cursor/debug.log

# Database Connection Pooling (PostgreSQL only)
# DB_POOL_SIZE=10
# DB_MAX_OVERFLOW=20
# DB_POOL_RECYCLE=3600

# API Configuration
# Optional: API secret keys, JWT secrets, etc.
# API_SECRET_KEY=your-secret-key-here
